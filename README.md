# Dead-Code-Elimination
A project that eliminates the deadcodes (the parts of the code that are irrelevant) from the input code and gives the dead code eliminated version of the code as an output. There is also an input file in the project that you can try the dead code elimination with.
